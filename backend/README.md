# backend

json-server의 `Service`를 감싼 실습용 API 서버입니다. 기본 CRUD 동작은 유지하되, `DELETE /posts/:id`는 `204 No Content`를 반환하도록 맞춰 두었습니다.

## 실행

```bash
pnpm install
pnpm dev
```

기본 주소는 `http://localhost:4000`입니다.
